<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.antoinecopiecolle">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- Permission camera + stockage externe pour la photo -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mma"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailsMatch"
            android:label="@string/details"
            android:parentActivityName=".Historique"
            android:usesCleartextTraffic="true">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.antoinecopiecolle.DetailsMatch" />
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCmogzet43jQ0w8uqUNHA_D_JcdooErfKM" />

        <activity
            android:name=".Maps"
            android:label="@string/title_activity_maps"
            android:parentActivityName=".Camera"
            android:usesCleartextTraffic="true">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.antoinecopiecolle.Maps" />
        </activity>
        <activity
            android:name=".Camera"
            android:label="@string/camera"
            android:parentActivityName=".Combat"
            android:usesCleartextTraffic="true">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.antoinecopiecolle.Camera" />
        </activity>
        <activity
            android:name=".Historique"
            android:label="@string/historique"
            android:parentActivityName=".MainActivity"
            android:usesCleartextTraffic="true">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.antoinecopiecolle.Historique" />
        </activity>
        <activity
            android:name=".Combat"
            android:label="@string/Combat"
            android:parentActivityName=".Preparation"
            android:usesCleartextTraffic="true">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.antoinecopiecolle.Preparation" />
        </activity>
        <activity
            android:name=".Preparation"
            android:label="@string/preparation"
            android:parentActivityName=".MainActivity"
            android:usesCleartextTraffic="true">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.antoinecopiecolle.MainActivity" />
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>